# Delegations

Each directory tracks one parent-to-child work delegation from proposal through child acceptance and
result collection. Delegation records reference swarm work; they do not duplicate child artifacts.
